# ==============================================================================
# ERPNext + SRI Docker Compose — Activación directa desde la raíz del proyecto
# ==============================================================================
# Uso:
#   docker compose build       (primera vez — construye imagen con SRI)
#   docker compose up -d       (levanta todo)
#   Espera ~2-3 min y abre http://localhost:8080
#   Usuario: Administrator | Contraseña: admin (o la definida en .env)
# ==============================================================================

x-custom-image: &custom_image
  image: erpnext-sri:${ERPNEXT_VERSION:-v16.5.0}
  platform: linux/amd64
  build:
    context: .
    dockerfile: Dockerfile
    args:
      ERPNEXT_VERSION: ${ERPNEXT_VERSION:-v16.5.0}

services:
  # ============================================================================
  # Base de datos — MariaDB
  # ============================================================================
  db:
    image: mariadb:10.6
    restart: unless-stopped
    healthcheck:
      test: mysqladmin ping -h localhost --password=${DB_PASSWORD:-admin}
      interval: 5s
      retries: 20
    command:
      - --character-set-server=utf8mb4
      - --collation-server=utf8mb4_unicode_ci
      - --skip-character-set-client-handshake
      - --skip-innodb-read-only-compressed
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD:-admin}
      MARIADB_ROOT_PASSWORD: ${DB_PASSWORD:-admin}
    volumes:
      - db-data:/var/lib/mysql
    networks:
      - erpnext-network

  # ============================================================================
  # Redis — Cache y Cola de trabajos
  # ============================================================================
  redis-cache:
    image: redis:6.2-alpine
    restart: unless-stopped
    networks:
      - erpnext-network

  redis-queue:
    image: redis:6.2-alpine
    restart: unless-stopped
    volumes:
      - redis-queue-data:/data
    networks:
      - erpnext-network

  # ============================================================================
  # Configurador — Inicializa las configuraciones de Bench
  # ============================================================================
  configurator:
    <<: *custom_image
    restart: on-failure
    entrypoint:
      - bash
      - -c
    command:
      - >
        ls -1 apps > sites/apps.txt; bench set-config -g db_host $$DB_HOST; bench set-config -gp db_port $$DB_PORT; bench set-config -g redis_cache "redis://$$REDIS_CACHE"; bench set-config -g redis_queue "redis://$$REDIS_QUEUE"; bench set-config -g redis_socketio "redis://$$REDIS_QUEUE"; bench set-config -gp socketio_port $$SOCKETIO_PORT;
    environment:
      DB_HOST: db
      DB_PORT: "3306"
      REDIS_CACHE: redis-cache:6379
      REDIS_QUEUE: redis-queue:6379
      SOCKETIO_PORT: "9000"
    depends_on:
      db:
        condition: service_healthy
      redis-cache:
        condition: service_started
      redis-queue:
        condition: service_started
    volumes:
      - sites:/home/frappe/frappe-bench/sites
      - logs:/home/frappe/frappe-bench/logs
    networks:
      - erpnext-network

  # ============================================================================
  # Creador de sitio — Crea el sitio ERPNext + SRI automáticamente
  # ============================================================================
  create-site:
    <<: *custom_image
    restart: "no"
    entrypoint:
      - bash
      - -c
    command:
      - >
        wait-for-it -t 120 db:3306; wait-for-it -t 120 redis-cache:6379; wait-for-it -t 120 redis-queue:6379; export start=`date +%s`; until [[ -n `grep -hs ^ sites/common_site_config.json | jq -r ".db_host // empty"` ]] &&
          [[ -n `grep -hs ^ sites/common_site_config.json | jq -r ".redis_cache // empty"` ]] &&
          [[ -n `grep -hs ^ sites/common_site_config.json | jq -r ".redis_queue // empty"` ]];
        do
          echo "Esperando sites/common_site_config.json...";
          sleep 5;
          if (( `date +%s`-start > 120 )); then
            echo "Timeout: no se encontró common_site_config.json con las claves requeridas";
            exit 1;
          fi;
        done; echo "Configuración encontrada. Creando sitio ERPNext + SRI..."; bench new-site --mariadb-user-host-login-scope='%' --admin-password=${SITE_ADMIN_PASSWORD:-admin} --db-root-username=root --db-root-password=${DB_PASSWORD:-admin} --install-app erpnext --set-default frontend; echo "Instalando app SRI..."; bench --site frontend install-app sri; echo "SRI instalado correctamente!";
    volumes:
      - sites:/home/frappe/frappe-bench/sites
      - logs:/home/frappe/frappe-bench/logs
    depends_on:
      configurator:
        condition: service_completed_successfully
    networks:
      - erpnext-network

  # ============================================================================
  # Backend — Servidor de aplicación Frappe/ERPNext
  # ============================================================================
  backend:
    <<: *custom_image
    restart: unless-stopped
    volumes:
      - sites:/home/frappe/frappe-bench/sites
      - logs:/home/frappe/frappe-bench/logs
      - ./ErpSRI:/home/frappe/frappe-bench/apps/sri
    depends_on:
      configurator:
        condition: service_completed_successfully
    networks:
      - erpnext-network

  # ============================================================================
  # Frontend — Nginx reverse proxy
  # ============================================================================
  frontend:
    <<: *custom_image
    restart: unless-stopped
    command:
      - nginx-entrypoint.sh
    environment:
      BACKEND: backend:8000
      FRAPPE_SITE_NAME_HEADER: frontend
      SOCKETIO: websocket:9000
      UPSTREAM_REAL_IP_ADDRESS: 127.0.0.1
      UPSTREAM_REAL_IP_HEADER: X-Forwarded-For
      UPSTREAM_REAL_IP_RECURSIVE: "off"
      PROXY_READ_TIMEOUT: ${PROXY_READ_TIMEOUT:-120}
      CLIENT_MAX_BODY_SIZE: ${CLIENT_MAX_BODY_SIZE:-50m}
    volumes:
      - sites:/home/frappe/frappe-bench/sites
      - logs:/home/frappe/frappe-bench/logs
    ports:
      - "${HTTP_PORT:-8080}:8080"
    depends_on:
      - backend
      - websocket
    networks:
      - erpnext-network

  # ============================================================================
  # WebSocket — Comunicación en tiempo real
  # ============================================================================
  websocket:
    <<: *custom_image
    restart: unless-stopped
    command:
      - node
      - /home/frappe/frappe-bench/apps/frappe/socketio.js
    environment:
      FRAPPE_REDIS_CACHE: redis://redis-cache:6379
      FRAPPE_REDIS_QUEUE: redis://redis-queue:6379
    volumes:
      - sites:/home/frappe/frappe-bench/sites
      - logs:/home/frappe/frappe-bench/logs
    depends_on:
      configurator:
        condition: service_completed_successfully
    networks:
      - erpnext-network

  # ============================================================================
  # Workers — Procesamiento de colas de trabajo
  # ============================================================================
  queue-short:
    <<: *custom_image
    restart: unless-stopped
    command: bench worker --queue short,default
    volumes:
      - sites:/home/frappe/frappe-bench/sites
      - logs:/home/frappe/frappe-bench/logs
      - ./ErpSRI:/home/frappe/frappe-bench/apps/sri
    environment:
      FRAPPE_REDIS_CACHE: redis://redis-cache:6379
      FRAPPE_REDIS_QUEUE: redis://redis-queue:6379
    depends_on:
      configurator:
        condition: service_completed_successfully
    networks:
      - erpnext-network

  queue-long:
    <<: *custom_image
    restart: unless-stopped
    command: bench worker --queue long,default,short
    volumes:
      - sites:/home/frappe/frappe-bench/sites
      - logs:/home/frappe/frappe-bench/logs
      - ./ErpSRI:/home/frappe/frappe-bench/apps/sri
    environment:
      FRAPPE_REDIS_CACHE: redis://redis-cache:6379
      FRAPPE_REDIS_QUEUE: redis://redis-queue:6379
    depends_on:
      configurator:
        condition: service_completed_successfully
    networks:
      - erpnext-network

  # ============================================================================
  # Scheduler — Tareas programadas
  # ============================================================================
  scheduler:
    <<: *custom_image
    restart: unless-stopped
    command: bench schedule
    volumes:
      - sites:/home/frappe/frappe-bench/sites
      - logs:/home/frappe/frappe-bench/logs
      - ./ErpSRI:/home/frappe/frappe-bench/apps/sri
    depends_on:
      configurator:
        condition: service_completed_successfully
    networks:
      - erpnext-network

  # ============================================================================
  # SRI Downloader — Descarga masiva de documentos del SRI
  # ============================================================================
  sri-downloader:
    build:
      context: .
      dockerfile: Dockerfile.sri-downloader
    platform: linux/amd64
    restart: unless-stopped
    ports:
      - "${SRI_DOWNLOADER_PORT:-4000}:4000"
    environment:
      PORT: 4000
      N8N_WEBHOOK_URL: ${N8N_WEBHOOK_URL:-}
      WINDOWS_API_URL: ${WINDOWS_API_URL:-}
      SRI_USER: ${SRI_USER:-}
      SRI_PASS: ${SRI_PASS:-}
      FRAPPE_URL: http://frontend:8080
      ERPNEXT_API_KEY: ${ERPNEXT_API_KEY:-}
      ERPNEXT_API_SECRET: ${ERPNEXT_API_SECRET:-}
      ERPNEXT_URL: http://frontend:8080/api/method/sri.sri.doctype.importar_documentos_sri.importar_documentos_sri.recibir_respuesta_n8n
    depends_on:
      - frontend
    networks:
      - erpnext-network

  # ============================================================================
  # SRI API — Facturacion electronica .NET (GeneradorPDF)
  # ============================================================================
  sri-api:
    build:
      context: ./GeneradorPDF
      dockerfile: Yachasoft.Sri.FacturacionElectronica/Dockerfile
    platform: linux/amd64
    restart: unless-stopped
    ports:
      - "${SRI_API_PORT:-5000}:5000"
    environment:
      ASPNETCORE_URLS: http://+:5000
      ASPNETCORE_ENVIRONMENT: Production
    networks:
      - erpnext-network

# ==============================================================================
# Volumenes persistentes
# ==============================================================================
volumes:
  db-data:
  redis-queue-data:
  sites:
  logs:

    # ==============================================================================
    # Red interna
    # ==============================================================================
networks:
  erpnext-network:
    driver: bridge
